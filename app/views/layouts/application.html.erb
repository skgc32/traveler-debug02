<!DOCTYPE html>
<html>
  <head>
    <title>MOVIE RECODE</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %> 
      <header>
        <div><%= link_to root_path, class: "text-white nav-link" do %>traveler<% end %></div>
        <div><%= link_to "トップ", root_path, class: "text-white nav-link" %></div>
        <div><%= link_to "レビュー", new_post_path, class: "text-white nav-link" %></div>
        <div><%= link_to "ユーザーレビュー", posts_path, class: "text-white nav-link" %></div>
        <div><%= link_to "ウォッチャリスト", confirm_posts_path, class: "text-white nav-link" %></div>
        <div><%= link_to "ユーザー一覧", users_path, class: "text-white nav-link" %></div>
        <div><%= link_to "マイレビュー", user_path(current_user.id), class: "text-white nav-link" %></div>
        <div><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white nav-link" %></div>
      </header>
    <% end %>
    <%= yield %>
    <footer>© 2025 MOVIE RECODE</footer>
  </body>
</html>
